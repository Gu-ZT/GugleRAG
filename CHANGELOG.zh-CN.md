# 更新日志

GugleRAG 的重要变更记录在此文件中。

## [0.2.0] - 2026-07-31

- 添加数据库中立的持久化文档嵌入与余弦相似度向量检索。
- 接入 SiliconFlow 与本地 OpenAI 兼容嵌入服务，默认完整调用地址为 `https://api.siliconflow.cn/v1/embeddings`。
- 添加可选的 SiliconFlow、本地和自定义 HTTP 重排服务。
- 启动或首次搜索时自动为升级前创建的文档建立索引，文档编辑后按内容哈希自动重建向量。
- 更新初始化向导、管理员检索配置、集成测试与检索文档。

## [0.1.1] - 2026-07-31

- 添加管理员用户管理，可查看用户列表、用户所属工作区，并新增、修改或删除账号。
- 添加公开注册开关，关闭后只能由管理员新增用户，注册页显示“注册已关闭，请联系管理员添加”。
- 添加管理员配置与应用内服务重启控制。
- 添加数据库方言支持、文档夹和 ZIP 文档导入。
- 添加跨工作区知识库搜索，并优化工作区文档树处理。
- 添加网站图标、pnpm 包管理器元数据、更多 GitHub Markdown 警告框与脚注支持，以及知识库删除。
- 添加首次初始化写入 `.env` 后自动重启服务。
- 为发布说明中的构建产物和校验文件添加 GitHub Release 直接下载链接。

## [0.1.0] - 2026-07-30

- 建立 Rust 与 Vue 3 项目结构，并提供分步首次启动配置流程。
- 通过数据库中立的 SQLx 查询支持 SQLite、MySQL 和 PostgreSQL 配置。
- 支持个人与团队工作区、多知识库、成员邀请和文档编辑。
- 提供带工作区及知识库发现工具的范围化 MCP 访问。
- 支持说明、警告和 GitHub 风格任务列表的 Markdown 预览。
- 为 Linux、Windows、macOS 的 x64 与 ARM64 目标加入架构校验、多平台 CI 和便携发布包流程。
- 支持主分支自动发布带唯一构建标签的 prerelease，并通过版本标签发布正式版本。
